import 'package:flutter/material.dart';

class DotsDecorator {
  final Color color;
  final Size size;
  final Color activeColor;
  final Size activeSize;

  const DotsDecorator({required this.color, required this.size, required this.activeColor, required this.activeSize});
}
